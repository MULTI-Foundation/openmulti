# ENGINEERING BACKLOG — OpenMulti ecosystem

*Trous d'ingénierie découverts pendant la mission de recherche, pour réimplémentation ultérieure. Maintenu en continu ; chaque item cite ses preuves (`file:line` dans les copies `source/`). Règle Zéro : rien n'est modifié dans les dépôts d'origine — les correctifs se font ici (copies de recherche) ou par le PI en amont.*

*Créé le 2026-07-05 à partir de la cartographie Phase 1 (17 rapports d'agents + critique). Statuts : `open` / `fixed-in-research` / `upstreamed`.*

---

## P0 — Bugs de correction et failles de sûreté

| # | Problème | Preuve | Correctif proposé | Statut |
|---|---|---|---|---|
| P0-1 | **Le devis "garanti" peut sous-estimer la facture** : Qwen tarifé au tier de contexte de base et au taux output non-thinking ; la dimension output n'a AUCUN coussin (outputMax×rate, dollar pour dollar). Un devis `/v1/plan`/x402 sur gros contexte ou thinking undershoot. | `pricing.ts:69-73`, `plan.ts:8-11` | Modéliser les tiers de contexte et taux thinking dans la table, OU refuser de quoter (`pricing_unknown`) les vendors à tarification par paliers. Documenter l'enveloppe de validité. | ✅ done-in-prod (commit d056278) — flag `tiered: true` porté sur les DEFAULTS `pricing.ts` (9 qwen + qwen3-coder-plus) ; `computeQuote` refuse → `pricing_tiered` (propagé à plan/programme/council/x402, un seul chemin). Le code de refus existait depuis Étage 2 mais AUCUN modèle n'était flaggé (trou vivant). Facturation best-effort inchangée (prix de base, `computeCostUsd` non gardé). Tests : `plan.test.ts` (tiered→refus, non-flaggé→quotable, /v1/plan 200 sans jeton) + `pricing.test.ts` (smoke deepEqual strict verrouille le flag). Cf [[Q-1]] (même chemin, taux thinking). |
| P0-2 | L'invariant "≤1 token/byte (byte-BPE)" marqué GARANTI n'est vérifié contre aucun tokenizer déployé ; faux pour les tokenizers à normalisation NFKC. | `plan.ts:8-11` | Suite de tests de majoration par tokenizer déployé (tiktoken, Anthropic, Qwen BBPE) sur corpus adversarial (CJK, emoji, code dense). | fixed-in-research — harnais `phase4/formal/axiom-harness/` : axiome VALIDÉ tiktoken o200k/cl100k, **VIOLÉ pour Qwen (1.33 tok/byte)** → Qwen classé C/refusé (cohérent P0-1) ; statut par provider daté dans AXIOM_STATUS.md | ✅ done-in-prod (verrou, commit 867406e) — la mitigation prod = P0-1 (tout Qwen refusé au devis via `tiered`, donc la borne-octets n'est JAMAIS utilisée sur le seul provider où l'axiome est violé) ; les autres modèles tarifés sont tiktoken-family (axiome validé). Test `plan.test.ts` « P0-2 » : énumère `qwen/*` tarifés et exige `pricing_tiered` de `computeQuote` → un flag perdu (Qwen requotable, sous-estimant) casse le test, pas la prod. Cf [[P0-1]]. |
| P0-3 | Council `deliberate` envoie la ronde de review au panel COMPLET, y compris les membres qui ont échoué à l'étape 1 (map sur `panel`, pas `okPanel`) — appels dépensés sur des modèles morts. | `council.ts:122` | Mapper sur `okPanel`. | ✅ done-in-prod (branche etage0-p0-fixes) — `council.ts` mappe sur `okPanel` ; test `council.test.ts` « P0-3 … QUE les survivants » (spy : cheap2 mort jamais re-sollicité en review). |
| P0-4 | Fallback d'échec du chair retourne `texts[0]` (premier survivant dans l'ordre de config) alors que le commentaire prétend "meilleure réponse" — aucun signal de qualité. | `council.ts:140-143` | Au minimum corriger le commentaire ; mieux : fallback sur un score (longueur/latence/second chair). | ✅ done-in-prod (etage0) — fallback = réponse survivante la PLUS LONGUE (signal de contenu) + commentaire honnête ; test `council.test.ts` « P0-4 … la PLUS LONGUE ». |
| P0-5 | La route embeddings ne re-mesure jamais le body bufferisé (contrairement à chat) → un Content-Length menteur/absent contourne la limite de taille. | `routes/embeddings.ts:56` | Répliquer la re-mesure de `chat.ts:63-66`. | ✅ done-in-prod (etage0) — re-mesure `c.req.text()` + `Buffer.byteLength` répliquée ; tests `embeddings.test.ts` (body streamé sans Content-Length > limite → 413 sans appel upstream ; body normal streamé passe). |
| P0-6 | `record()` garde par truthiness (`if (rec.costUsd)`) : un coût légitime de 0 ne produit ni billedUsd ni metering facturé. | pipeline (chat.ts) | Tester `!= null` au lieu de la truthiness. | ✅ done-in-prod (etage0) — `!= null` sur TOUTE la chaîne de facturation (route `chat.ts`+`embeddings.ts`, `meter.ts`, `metrics.ts` — le fork n'avait corrigé que la route, laissant `meterUsage` re-escamoter le 0) ; test `meter.test.ts` « P0-6 … metré (série à 0) ». |
| P0-7 | Stream : `cancel` n'enregistre AUCUNE observation, et un client qui ne lit jamais jusqu'au bout diffère l'observation indéfiniment — trou dans le signal du bandit. | `routes/chat.ts` (branche stream) | Enregistrer l'observation (partielle, marquée) sur cancel/timeout. | ✅ done-in-prod (etage1, **version minimale** — décision mainteneur 2026-07-09) — `record()` sur `cancel()` avec l'usage partiel scanné (error:false, `aborted:true` au log), guard `observed` anti-double-record. **Backpressure INCHANGÉ** (garde `pull()`). Test `stream-observe.test.ts`. **RESTE** (non retenu ici) : le cas « client qui ouvre le stream mais cesse de lire sans cancel » diffère toujours l'observation (le fix fork `pull()`→`start()` le couvrirait mais change le backpressure — écarté). |
| P0-8 | Le transcodeur streaming Anthropic transforme une erreur de lecture mi-stream en terminaison d'apparence normale (`catch → done:true`) — un stream tronqué est indiscernable d'un complet. | `providers/anthropic.ts:165` | Propager un événement d'erreur SSE ou fermer avec un finish_reason distinct. | ✅ done-in-prod (etage1) — `readError` dans `anthropic.ts` → chunk final `finish_reason:'error'` + objet `error` + `log.warn` ; `SseUsageScanner.errored` (`sse.ts`) détecte objet error / finish_reason error ; branche `done` de `chat.ts` : `error = stalled || scanner.errored` → observation d'erreur pour le bandit. Tests : `sse.test.ts` (unité errored) + `anthropic.test.ts` (e2e stream tronqué). |
| P0-9 | `MIN_SAMPLES ≥ DECAY_WINDOW` rend l'exploration permanente (round-robin pur) sans garde, log ni métrique. | `select.ts`, dérivation gap:3 | Garde de config au boot + métrique d'alerte "collapsed regime". | ✅ done-in-prod (etage0) — `banditRegimeCollapsed()` (pure) + garde de boot dans `select.ts` (log.warn `bandit_collapsed_regime`) + jauge Prometheus `openmulti_bandit_collapsed_regime` ; tests `bandit-regime.test.ts`. |
| P0-11 | **La boucle retry/failover peut re-dispatcher un appel SOUS CONTRAT** : une exception transport post-envoi (timeout, reset en lecture) laisse l'état de facturation provider inconnu ; le retry (jusqu'à maxRetries+1 tentatives × N chemins) peut facturer le même étage 2×+ et dépasser le montant signé SANS violer aucun axiome d'enveloppe. Trouvé par revue externe 2026-07-07, invisible à la batterie TOCTOU (attaques toutes single-dispatch). | `routes/chat.ts:234-281` (et copie `forward.ts:66-90` pour le council, hors contrat) | Sous contrat (`pin` présent) : AU PLUS UN dispatch — exception transport → `504 contract_no_retry` ; statut transitoire reçu → erreur normalisée statut préservé, ni retry ni failover. Sans contrat : comportement historique inchangé. Council Σ-quote : reste à borner (P3). | ✅ done-in-prod (etage2 tranche 4) — débloqué par le `pin` E-1 ; gardes dans la boucle retry de `chat.ts` (exception transport → 504 `contract_no_retry` ; 503 reçu → break, statut préservé) ; test `contract-no-retry.test.ts` (1 dispatch sous contrat vs 3 sans). |
| P0-10 | Dans le régime effondré, le filtre de santé est inerte : la branche explore précède le check de taux d'erreur — un bras insuffisamment échantillonné ET malsain continue de recevoir 1/A du trafic. | `select.ts:52-61` | Appliquer le gate de santé aussi aux picks d'exploration (ou quarantaine explicite). | ✅ done-in-prod (etage0) — helper `isHealthy` partagé, exploration filtre les sous-échantillonnés SAINS (repli sur tous si aucun sain → guérison préservée, `bandit.test.ts` vert) ; tests `bandit-regime.test.ts` (aggregate injecté + boucle régime effondré). |

## P0-SEC — Sécurité / hygiène des secrets (dépôts privés inclus)

| # | Problème | Preuve | Correctif proposé | Statut |
|---|---|---|---|---|
| S-1 | **Secret HMAC des devis x402 mainnet hardcodé en clair** dans un script du dépôt ops, contredisant la doctrine "secrets seulement en k8s secrets". | `openmulti-ops/deploy/x402-prod-activate.sh:18`, vs `README.md:31-32` | Rotation immédiate du secret + lecture depuis l'env/secret k8s. | open |
| S-2 | `catalog.local.json` ("NE JAMAIS COMMITER") est présent dans l'arborescence du projet — le mapping d'exploitation réel est lisible. | `openmulti/catalog.local.json` | Vérifier le .gitignore effectif et l'historique git du vrai dépôt ; purger si commité. | open |
| S-3 | Items ouverts de l'audit 2026-07-02 : GitHub Actions non épinglées par SHA (risque RCE avec creds de deploy), API k3s staging :6443 publiquement joignable avec un SA token sans expiration, signatures cosign jamais vérifiées au rollout. | `openmulti-ops/docs/SECURITY-AUDIT-2026-07-02.md` | Épingler par SHA ; netpol/firewall sur :6443 + token à expiration ; vérification cosign en admission. | open |
| S-4 | Confiance de settlement mono-partie : le cross-verify durcit `verify` mais le succès de `settle` est cru sur parole du facilitateur primaire seul. | `x402-facilitator.ts:81` | Vérification on-chain du transfert (lecture du tx receipt) avant crédit. | open |
| S-5 | Anti-replay des nonces : un flush/failover du store dans la fenêtre de settlement (TTL 3600s) rouvre la fenêtre de replay (fail-closed couvre store DOWN, pas store WIPED). | `x402-gate.ts` | Persistance des nonces ou fail-closed sur redémarrage de store dans la fenêtre. | open |
| S-6 | `signup_verify` imprime la clé API dans la conversation MCP — elle transite par le contexte de l'agent. | `multi-lang/src/openmulti/mcp.py` | Livraison hors-bande (URL à usage unique pour l'humain). | open |

## P1 — Solidité économique / facturation

| # | Problème | Preuve | Correctif proposé | Statut |
|---|---|---|---|---|
| E-1 | **Le devis payé n'est PAS enforcé à l'exécution** (question ouverte n°1 de la recherche) : rien ne lie explain→run, pas d'argument max_cost sur le tool run, pas de pin serveur du modèle quoté ; TOCTOU devis→run si pricing/routing change. | `multi-lang/mcp.py`, `plan.ts`, `x402-gate.ts:222-227` | Faire du devis un contrat : le gateway rejette toute exécution dont la borne dépasse le devis payé (pin du SNAPSHOT DE CANDIDATS + caps, décision du réfuteur théorie). C'est AUSSI le maillon manquant de l'axe de recherche U3. Fait : `src/quote-token.ts` (jeton HMAC signé émis par /v1/plan, digest + candidats + caps + marge + version de table + montant + TTL), vérifié à l'exécution ET pré-vérifié sur le rail x402 avant tout règlement. | ✅ **done-in-prod (Étage 2, chemin CHAT complet)** — feu vert mainteneur sur `router.ts` (2026-07-09). Tranches : (1) fondation `quote-token.ts`+`program-quote.ts` + prérequis (router `route(req, constrainTo?)` + `RouteDecision.candidates` ; `plan.ts` `extraInputTokens` + refus P0-1/Q-1 ; `pricing.ts` `PRICING_TABLE_VERSION` + flags `tiered`/`thinking` ; `types.ts` ; config `OPENMULTI_QUOTE_TOKEN_SECRET`/TTL) ; (2) émission `/v1/plan` (chat + programme, jeton si guaranteed, ι épinglés) ; (3) enforcement `chat.ts` (décodage 422 / routage contraint / `checkPinnedQuote` chat / `checkPinnedProgramStage` étagé + garde pré-spend E-8 par borne OCTETS ; jeton jamais transmis upstream) ; (4) P0-11. Tests : quote-token/program-quote/quote-plan-emit/quote-enforce/contract-no-retry (32). **Étage 2 COMPLET** : pré-vérif rail **x402** livrée (`x402-gate.ts`, commit d5a6a54) ; **E-5** council Σ-quote livré (3d2071a) ; **E-8** garde tokenizer livrée (c0bdd7d). |
| E-2 | Trop-payé x402 : le transfert EIP-3009 déplace la valeur max signée on-chain ; le surplus n'est remboursé qu'en crédit interne, jamais au wallet. | `x402-gate.ts:228` | Documenter contractuellement, ou settle du montant exact/refund on-chain. | open |
| E-3 | Aucun chemin de refund/chargeback : seul `checkout.session.completed` est consommé ; un refund Stripe laisse les crédits gateway intacts. | `console/app/api/stripe/webhook/route.ts` | Consommer `charge.refunded` → débit gateway idempotent. | open |
| E-4 | Comptabilité en flottants de bout en bout (paidUsd = Number/1e6, HINCRBYFLOAT). | `x402-gate.ts:228`, meter | Passer en micro-USD entiers. | ✅ done-in-prod (etage1) — `micro-usd.ts` (usdToMicro/microToUsd, conversion aux frontières seulement) ; `meter.ts` écrit `cost_microusd`/`billed_microusd`/`spent:microusd` en HINCRBY entier ; `keys.ts` cumule en µ$ + **double-lecture** legacy `*_usd`/`spent:usd`/`credits:usd` (soldes pré-migration justes) via `mergeLedgers` ; ADD_CREDITS_LUA → HINCRBY entier ; `x402-gate.ts` `paidUsd = microToUsd(valueAtomic)` (round-trip exact, USDC atomique = µ$). Frontières (API balance/usage, readUsage) restent en USD. Tests : `micro-usd.test.ts` (conversions, non-dérive, double-lecture) + `meter.test.ts`/`balance.test.ts`/`x402-gate.test.ts` mis au schéma micro. **Non inclus** (couplé P0-7 fork) : le champ `aborted` du metering. |
| E-5 | Le council (surface la plus chère) est déclaré non-quotable sur x402 → impayable par une machine. | `x402-gate.ts:143`, `routes/plan.ts:46-50` | Quote de council = Σ bornes des N+1..2N+1 sous-appels (calculable : panel connu, caps connus). | ✅ done-in-prod (etage2, commit 3d2071a) — `src/council-quote.ts` (`computeCouncilQuote` : panel/chair résolus, `calls` = N+1 fuse / 2N+1 deliberate, borne = Σ des caps de sortie × prix, marge appliquée ; un membre non quotable propage `unavailable` — jamais de devis muet) ; `routes/plan.ts` branche council (200 + bloc council, remplace le refus 400). Tests : `council-quote.test.ts` (fuse N+1, deliberate 2N+1 ≥ fuse, propagation d'indisponibilité, /v1/plan 200). |
| E-6 | Enforcement caps/soldes éventuellement cohérent : dépassement possible d'un intervalle de refresh (10 s) × pods + 1 requête en vol par pod ; fail-open des clés trusted sur panne Redis. | pipeline, economics-infra | Documenter la borne de dépassement ; réservation atomique optionnelle pour les projets prépayés. | open |
| E-7 | Table de prix maintenue à la main, pas de sync avec le feed OpenRouter /models déjà fetché ; un prix périmé biaise silencieusement bandit ET devis (incident 2026-07-02). | `pricing.ts`, `openrouter-catalog.ts` | Job de sync + alerte de dérive prix table vs feed. | fixed-in-research |
| E-10 | **Le pont tokenizer de la garde retombe SILENCIEUSEMENT sur la borne octets si les tokenizers ne sont pas importables** : `count_tokens.py` rend `{"tokens": null}` quand `tiktoken`/`transformers`/`mistral_common` ne sont pas sur le `sys.path` (installés en `pip --target .deps`, jamais ajoutés au `PYTHONPATH` du pont) → `measureStageInput` retombe sur `byte_bound`. Conséquence : les runs guard-in-path live (`gwpath_*`, `xchain_live`) mesuraient en réalité la BORNE OCTETS, pas le tokenizer du provider ; leurs 409 se déclenchaient sur les octets. Toujours SOUND (byte_bound majore le compte de tokens byte-BPE), mais (a) la capacité « re-mesure via le tokenizer du provider » annoncée ne tournait pas dans ces runs, (b) un repli silencieux masque une misconfiguration en prod (déguise une garde lâche en garde serrée). Trouvé en construisant la passe A5 adversariale (`a5_adversarial_run.mjs`, 2026-07). | `fork/openmulti/src/stage-input-guard.ts:113-120` (repli), `phase4/formal/axiom-harness/bridge/count_tokens.py:31` (`sys.path` = harness seul, pas `.deps`) | Le `OPENMULTI_TOKENIZER_BRIDGE_CMD` de prod doit exporter `PYTHONPATH` vers les deps (ou le venv doit les contenir) ; le pont devrait DISTINGUER « pas de tokenizer pour ce modèle » (null légitime) de « tokenizers non importables » (misconfiguration → log bruyant / métrique / échec au boot), plutôt que rendre `null` dans les deux cas. La passe A5 fixe le `PYTHONPATH` côté harness et mesure enfin le vrai chemin tokenizer (M ≥ billed adversarialement, marge structurelle +20 tokens d'enveloppe JSON). | open (repli sound mais trompeur ; fix observabilité côté pont + doc de déploiement du `PYTHONPATH`) |
| E-11 | **Aucune mesure de performance du chemin devis/garde/settlement** (revue externe 2026-07-07) : (a) la garde par étage lance UN SUBPROCESS Python par étage gardé cross-tokenizer (pont `OPENMULTI_TOKENIZER_BRIDGE_CMD`) — coût par étage jamais mesuré ; le pont persistant est cité en future work du papier mais n'avait pas d'item ; (b) zéro latence p50/p99 de `computeQuote`/`computeProgramQuote` ni du settle-then-serve x402 nulle part (papier ni harnais) — lacune franche pour un papier système. | `fork/openmulti/src/stage-input-guard.ts` (spawn par étage) ; `paper/sections/*` (aucune mesure de perf) | (1) Bench offline du devis en fonction de \|P\| ; (2) bench garde : registre in-process vs pont subprocess vs pont PERSISTANT (à implémenter : process long-vécu stdin/stdout réutilisant `count_tokens.py`) ; (3) latence verify+settle du gate x402 en prod. Cible : sous-section perf du papier (plan WS-C, `submission/REVISION_PLAN.md`). | **mesuré (2026-07-08)** — harnais `phase4/eval/perf/` (`./run.sh`, OFFLINE, fork réel + pont réel) : devis per-call 3–72 µs p50 (0,5–32 kB) ; admission+devis du programme MAXIMAL 128×32=4096 étages ≈ 13 ms p50, linéaire en \|P\| ; garde repli octets ~8 µs ; **pont spawn-per-call 0,37–2,1 s p50 PAR étage gardé** (openai 0,37 s / deepseek 1,4 s / mistral 2,1 s) vs plancher pont persistant 0,08–0,83 ms à chaud (charge 1× 0,3–1,5 s) → 3–4 ordres de grandeur. Chiffres intégrés au papier (§ guarantee-is-cheap). RESTE open : implémentation du pont persistant + (3) latence x402 prod (LIVE_NEEDED). |
| E-8 | **AX-CHAIN sans garde runtime** (reviewers P0 ronde 1, point bloquant) : l'enforcement par étage re-vérifie caps/prix/candidats mais JAMAIS le compte de tokens de la valeur qui coule dans le pipe ; une violation du folding token→token (g=1) — ex. étage amont d'un autre vendor instruit d'émettre du texte fragmentant (emoji/ZWJ, exclusions NFC) — percerait le contrat signé en silence. | `fork/openmulti/src/program-quote.ts` (fold), `quote-token.ts` (enforcement par étage : pas de recompute du token count de l'input pipé) ; papier §5 « staged enforcement » | Garde pré-spend par étage : recompute `tokens(valeur)` (ou borne octets/proxy provider) et 409 si > ι du fold ; alternative : restreindre les pipes aux étages même-tokenizer. | ✅ done-in-prod (etage2, commit c0bdd7d) — `src/stage-input-guard.ts` (`measureStageInput` : tokenizer du provider via registre in-process, sinon pont `OPENMULTI_TOKENIZER_BRIDGE_CMD`, sinon repli borne OCTETS conservateur) câblé dans `routes/chat.ts` (`realInput = await measureStageInput(exec, decision.model)`) ; le compte RÉEL alimente le check (f) de `checkPinnedProgramStage` → 409 `stage_input_exceeds_bound` avant tout appel upstream ; config `stageInputGuard { bridgeCmd, bridgeTimeoutMs }`. Tests : `stage-input-guard.test.ts` (providerFamilyOf, repli byte_bound, tokenizer enregistré méthode+undefined→repli, intégration mesure tokenizer hors-borne → 409 à zéro appel upstream). Détail recherche ci-dessous. — fixed-in-research (2026-07-05) — le jeton programme épingle désormais les ι_i du fold (`claims.inputs`, `fork/openmulti/src/quote-token.ts` + émission `routes/plan.ts`) ; garde pré-spend par étage = check (f) de `checkPinnedProgramStage` : le compte de tokens d'entrée RÉEL de la requête d'étage (tokenizer du provider résolu — registre en process ou pont `OPENMULTI_TOKENIZER_BRIDGE_CMD` → `phase4/formal/axiom-harness/bridge/count_tokens.py`, loaders WS3 réutilisés ; sinon repli borne OCTETS du devis, conservateur jamais optimiste) est mesuré AVANT tout appel upstream (`src/stage-input-guard.ts`, câblé `routes/chat.ts`) → 409 `stage_input_exceeds_bound` si > ι_i, zéro dépense ; jeton programme pré-E-8 sans `inputs` = refus fail-closed. Vérifié bout-en-bout contre le vrai tokenizer DeepSeek via le pont (37 tokens mesurés vs ι₀=182). Tests : `fork/openmulti/test/stage-input-guard.test.ts` (8 : bénin same-tokenizer passe / valeur fragmentante 409 à ZÉRO appel upstream / repli conservateur sans tokenizer / legacy fail-closed / garde pure / protocole du pont) ; suite complète 357/357 verte. **Reste à faire côté papier : `paper/sections/enforcement-protocol.tex` doit ajouter le check (f) à l'énumération de l'enforcement étagé** (le contrat pipeline devient sound MÊME SI AX-CHAIN/g=1 est violé ; l'amendement A3 passe de « no runtime guard checks it » à « guarded ») — édition par l'agent papier, pas ici. E-9 (mesure adversariale des ratios g par paire) : fermé le même jour par `phase4/eval/xchain/` — la garde rend toute violation non-facturable ET la mesure existe (g_max par paire, amendement A4). |
| E-9 | **AX-CHAIN non mesuré par paire de providers** : aucun record adversarial du ratio de re-tokenization max `tokens_B(render(tokens_A))/|tokens_A|` sur les paires admises ; obligation pré-enregistrée par l'amendement A3 de l'enveloppe. | `phase4/formal/ENVELOPE.md` §8 amendement A3 ; harnais `phase4/formal/axiom-harness/` (corpus déjà prêts) | Étendre le harnais : re-tokeniser des sorties réelles (CJK/NFC lourds) et les corpus adversariaux sous chaque tokenizer validé, par paire ; si g>1 mesuré → constante par paire dans le fold (ratio en g^d) ou restriction même-tokenizer. | fixed-in-research (2026-07-05) — harnais `phase4/eval/xchain/` (1153 chaînes : corpus adversariaux WS3 + fuzz seedé 20260705 + registres réels WS5 + scan amplificateur ; cap d'étage 1024 tokens ; tokenizers déployés o200k / DeepSeek-V3 / GLM-4.5 / Tekken `mistral-common`) : **g=1 FALSIFIÉ sur les 12 paires cross-tokenizer** — g_max par paire : 8.000 (openai→deepseek, openai→mistralai, deepseek→mistralai, z-ai→deepseek, z-ai→mistralai ; pires cas `\r\n`×32 et U+FFFD×32), 6.000 (deepseek→z-ai, ligatures U+FB03), 4.000 (deepseek→openai), 3.000 (openai→z-ai, U+1F3FB seul), 2.000 (z-ai→openai, mistralai→deepseek, mistralai→z-ai), 1.143 (mistralai→openai, la plus douce). Pas seulement adversarial : registres RÉELS g jusqu'à 1.360 (CJK réel) et médiane 1.216 (deepseek→mistralai). Paires même-tokenizer : g=1 exact + excès de collage `prompt\n\nvaleur` = 0 token (4 downstreams). Caveat divulgué : encodage amont canonique ⇒ g_max = borne INFÉRIEURE mesurée du supremum adversarial ; paires anthropic/moonshot UNVALIDATED (aucun tokenizer local). Record daté + règle refuse/fold = amendement **A4** de `phase4/formal/ENVELOPE.md` (g=1 couvert même-tokenizer seulement ; cross → constante par paire datée (∏g) OU refus OU garde E-8 — la garde E-8 posée le même jour rend toute violation non-facturable, g_max devient une métrique de disponibilité). Sorties : `xchain/data/xchain_ratios.json`, `xchain/XCHAIN_RESULTS.md`, `xchain/figures/fig_xchain.pdf`. |

## P5 — Trous découverts en Phase 4 (évaluation)

| # | Problème | Preuve | Correctif proposé | Statut |
|---|---|---|---|---|
| Q-1 | **Enveloppe de soundness non câblée dans le fork** : les deltas E1–E3 (gemini implicit-thinking, deepseek-reasoner, GLM-thinking) sont refusés par l'ENVELOPE pré-enregistrée mais deepseek-reasoner reste *mécaniquement quotable* par le code → 1 modèle sur 20 en discordance code↔enveloppe (mesuré, non caché, par WS5). | `phase4/eval/EVAL_RESULTS.md` (E2), `fork/openmulti/src/pricing.ts` | Câbler les exclusions de devis E1–E3 (flag de refus type `pricing_thinking`) pour que le devis mécanique corresponde à l'enveloppe. | ✅ done-in-prod (commit d056278) — flag `thinking: true` porté sur les DEFAULTS `pricing.ts` (`deepseek/deepseek-reasoner`, `google/gemini-3.1-flash-lite`) → `computeQuote` refuse `pricing_thinking` (propagé plan/programme/council/x402) ; GLM reste quotable-conditionnel (non flaggé). Tests : `plan.test.ts` (thinking→refus, /v1/plan 200 sans jeton). Cf [[P0-1]] (même chemin, palier de contexte). Détail recherche ci-dessous. — fixed-in-research — flag `thinking` sur `pricing.ts` (E1 `google/gemini-3.1-flash-lite`, E2 `deepseek/deepseek-reasoner`) + refus `pricing_thinking` dans `computeQuote` → propagé à TOUS les sites de devis (plan / programme / council / x402, un seul chemin). E3 GLM = `CONDITIONAL_THINKING_OFF` (quotable-conditionnel, PAS refusé — conforme au verdict pré-enregistré `coverage.py`) : le refuser créerait un écart INVERSE. Propriété WS1 étendue (`test/quote-soundness.test.ts`) : pour tout modèle tarifé, refusé (tiered/thinking/unpriced) OU sain ; ET ensemble mécaniquement quotable == ensemble non-REFUSED de l'enveloppe (zéro écart, deux sens). Eval re-run OFFLINE : `coverage_summary.json` → `envelope_vs_mechanical_discrepancies:[]`, `over_refusals:[]`, `mechanical_quotable_equals_envelope_included:true` ; sonde brute `chat_quote` ajoutée au harnais → E1 (gemini, id à point) démontré fermé sur `/v1/plan` brut (plus « masqué »). `fig_coverage.pdf` régénéré. Suites vertes (fork 349, multi-lang 160). |
| Q-2 | **7 divergences dual-door** (H8) trouvées au fuzzing 4000-seed : 3 idents à `\n` final, 3 champs inconnus, 1 lone-surrogate — la porte AST et la porte texte ne s'accordent pas sur accept/reject de ces entrées (le gateway est du côté strict sauf un crash Python sur surrogate). Zéro violation d'octets/idempotence, mais l'équivalence accept/reject n'est pas totale. | `phase4/adversarial/results/`, `dual_door_fuzz.py` | Aligner les deux portes sur ces classes (normaliser idents, rejet symétrique des champs inconnus, gestion du lone-surrogate) OU caractériser formellement le surplus comme H8 one-sided borné et le documenter comme limitation. | fixed-in-research — WS6 : porte AST de réf alignée sur le scanner texte et le gateway. (1) `_IDENT_RE` ancre en `\Z` (plus `$`) → idents à `\n` final rejetés partout ; (2) `_unknown_key` rejette les champs inconnus à chaque niveau (miroir de `unknownKey` du gateway, direction sûre = rejet) ; (3) `_is_utf8_text` rejette proprement un prompt à substitut UTF-16 isolé (plus de crash 500 sur MCP `program`), gateway aligné via regex `LONE_SURROGATE`. Fuzz 4000-seed re-run : **0 divergence** (`xdoor`/`h8_embed`/`idempotence`/`xcanon`/crashes). Verrous : `test_canonical.py::TestAlignementDesPortes`, `program-quote.test.ts`. Propriété restatée `H8_EQUIVALENCE.md` §P5. |
| Q-3 | Sous `OFFLINE=1`, le loader tokenizer Moonshot tente encore un fetch HF (échoue en UNVALIDATED, aucune donnée fabriquée, mais la promesse "aucun fetch réseau" est légèrement sur-vendue). | `phase4/eval/run.sh`, `tightness_tokenizer_status.json` | Court-circuiter le fetch Moonshot en mode OFFLINE (fail direct en UNVALIDATED). | open |
| Q-4 | Back-port du correctif Tekken (loader `mistral-common` au lieu de la conversion `transformers` qui perd le CJK) vers le harnais WS3. | `phase4/eval/data/mistral_tekken_axtok_recheck.json` | Basculer le loader mistral dans `phase4/formal/axiom-harness/harness/providers.py`. | open |
| Q-5 | **Innommabilité accidentelle des ids à point** : l'alphabet d'idents v0.1 (`[A-Za-z][A-Za-z0-9_/-]*`) n'admet pas `.` → 17/44 modèles tarifés inépinglables depuis un programme MULTI (`gpt-4.1`, `glm-4.5`, `kimi-k2.6`…) ; engagement papier « v0.2 grammar with re-frozen golden fixtures recovers every dotted id » (discussion-limitations-conclusion.tex:92). | `phase4/eval/EVAL_RESULTS.md` §1.1 (17/44), `fork/multi-lang/src/openmulti/parser.py:95-96` (v0.1) | Migration versionnée v0.2 : `.` en position intérieure seulement (ni initial/final/doublé), fixtures v0.1 gelées conservées + fixtures v0.2 gelées ajoutées, même règle sur les 3 portes. | ✅ done-in-prod (côté gateway, porté avec Étage 2) — la **porte de devis** `program-quote.ts:87` implémente la grammaire v0.2 (`^[A-Za-z][A-Za-z0-9_/-]*(?:\.[A-Za-z0-9_/-]+)*$`, ancre `$` = même règle que `\Z` de la porte AST réf) : les ids à point (`kimi-k2.6`…) sont nommables et donc REFUSABLES proprement à l'enveloppe (jamais masqués). Test `program-quote.test.ts` « grammaire v0.2 — ident pointé intérieur admis » (accepte `moonshotai/kimi-k2.6` + emoji-paire, rejette `.x`/`x.`/`a..b`). **RESTE côté consommateur** (MyMULTI, hors ce repo) : le scanner texte `multi-lang/parser.py` (`_IDENT_CONT_V02`, `GRAMMAR_VERSION`) + fixtures golden — c'est la porte langage de MyMULTI, pas du gateway. Détail recherche ci-dessous. — fixed-in-research (2026-07-06) — grammaire v0.2 : scanner texte (`parser.py`, `_IDENT_CONT_V02` + code stable `ident_dot_malformed`, `GRAMMAR_VERSION="0.2"`), porte AST réf (`runtime.py` `_IDENT_RE` = `^[A-Za-z][A-Za-z0-9_/-]*(?:\.[A-Za-z0-9_/-]+)*\Z`), porte gateway (`fork/openmulti/src/program-quote.ts:87` même regex, ancre `$`). Fixtures : `golden.json` INTOUCHÉ (18, bit à bit vert) + `fixtures/golden_v02.json` (8 : G14–G17, E6–E9) ; skills FR/EN régénérés des DEUX fichiers (`scripts/gen_skill.py`), verrou octet-à-octet re-figé (`test_skill_sync.py`). Note de migration : `fork/openmulti/docs/MULTI-Grammar-v0.2-Migration.md` (le doc v0.1 n'est pas réécrit). E1 DÉMASQUÉ, pas récupéré : gemini nommable → `refused: pricing_thinking` à la porte langage (verrou `test/program-quote.test.ts` « v0.2 démasque E1 ») ; E2 deepseek-reasoner : déjà câblé par Q-1 (id sans point, indépendant), rien de dû. Couverture re-run (`coverage_*_v02.csv`, `coverage_summary_v02.json`, v0.1 conservés) : porte langage 19→33 quotés, 17→0 innommables, == surface brute (33/9 tiered/2 thinking), écarts `[]` deux sens. Adversarial WS4 re-run avec `.` dans le fuzz (4000 scripts) : 0 divergence/violation/crash, grammar-bypass 15 cas 0 slip (`results/adversarial-2026-07-06.json`). Suites : fork **359** verts + typecheck, multi-lang **175** verts. |

## P2 — Ingénierie du routage / bandit

| # | Problème | Preuve | Correctif proposé | Statut |
|---|---|---|---|---|
| R-1 | **État du bandit in-memory par pod alors que prod tourne à replicas:2** → apprentissage split-brain, cold-start à chaque rollout. Invalide toute affirmation empirique tant que non centralisé. | `metrics.ts:6`, `openmulti-ops/deploy/prod.yaml:9` | Centraliser (Redis) ou sticky-partitionner les tiers par replica ; a minima persister l'état au shutdown. | open |
| R-2 | Décote par observation GLOBALE : le trafic d'un tier accélère l'oubli des autres ; le council vieillit tous les bras (ρ^(2N+1)) ; régime d'effondrement sous q < q_c ≈ A·M/W. | `metrics.ts:204-219`, gap:2/gap:3 | Tick de décote par candidate-set (ou par tier), pas global. Exclure les sous-appels council du tick global (ou flag). | open |
| R-3 | Récompense = coût brut/requête, non normalisée par tokens ; pas de latence (collectée mais inutilisée) ni de qualité. | `select.ts`, `metrics.ts` | Coût par token ; terme de latence ; hooks de signal qualité (probes judge) — recoupe l'axe recherche U1. | open |
| R-4 | Candidats de chemin hardcodés à exactement 2 ([direct, openrouter]). | `router.ts` (pathsFor) | Généraliser à N chemins. | open |
| R-5 | Pin `allow[0]` non validé (existence/pricing) ; `looksConcrete` fait confiance à tout string contenant '/'. | routing-core | Valider contre le catalogue au parse. | open |
| R-6 | Gate de santé sur estimation ponctuelle e/n vs 0.2, sans intervalle de confiance : un seul échec peut éjecter un bras juste au-dessus de MIN_SAMPLES. | `select.ts:61` | Borne de Wilson/Hoeffding au lieu du point estimate. | open |

## P3 — Robustesse & produit (sélection)

- **Council** : pas de timeout par membre ; panel non dédupliqué (le chair peut siéger au panel) ; les reviewers notent leur propre réponse (pas d'auto-exclusion) ; labels d'anonymisation en ordre fixe (Response A = même modèle à chaque requête) ; `max_tokens` copié tel quel au chair (le budget du caller cape la synthèse) ; usage des reviews échouées non compté ; streaming → 400. `council.ts:89,122,140`, `routes/chat.ts:82-84`.
- **Pipeline** : `forward.ts` copie assumée de la boucle de `chat.ts` (dérive possible) ; pas de deadline de bout en bout pour un stream lent-mais-sain (stall = 60-70 s) ; failover impossible après le premier byte ; avec margin>1 le stream n'est plus byte-identique (re-sérialisation JSON) ; `headerSafe` mutile l'Unicode légitime des headers.
- **Catalogue** : lu une fois au boot (changements de fichier = restart) ; overrides fail-open sans TTL sur panne Redis prolongée ; dualité d'orthographe des ids Anthropic patchée par duplication d'entrées.
- **multi-lang** : exécution strictement séquentielle (même pipelines indépendants) ; budget de run vérifié seulement ENTRE étages (dépassement possible d'un étage entier) ; slots sans caps de taille (croissance non bornée en REPL) ; ~~`explain` = 32 round-trips séquentiels sans cache~~ **fixed-in-research** (WS2 : UN aller-retour `/v1/plan` programme — bornes par étage + total garanti + jeton E-1 ; `run --max-cost`/`max_cost_usd` exécute sous contrat, jeton rejoué par étage) ; ~~skill/tools en français uniquement (confound + friction agents non francophones)~~ **partiellement fixed-in-research** (WS5 : `SKILL_EN.md` + `_skill_en.py` générés des mêmes fixtures, même verrou octet-à-octet, `tests/test_skill_sync.py` ; RESTE : descriptions des outils MCP et messages d'erreur runtime en français, et l'outil `learn` ne sert que le FR — à brancher au moment de l'étude EVAL_DOCS_PROTOCOL) ; collage prompt+input fixe `"prompt\n\ninput"` sans séparation structurelle (injection inter-étages) ; ~~comptabilité d'octets divergente entre portes (H8)~~ **fixed-in-research** (mesure canonique commune `canonical_program_bytes`, facteur 91 dérivé+testé, portes texte/AST/MCP/gateway alignées).
- **Console** : pricing des modèles présent dans le payload mais affiché nulle part dans l'UI ; `listKeys` O(total des clés) filtré côté client ; top-up public sans vérification d'existence du projet ; pas d'orgs/teams ; playground non-streamé ; rotation AUTH_SECRET invalide silencieusement les clés playground chiffrées.
- **Ops** : dérive de config par construction (CI = `kubectl set image` seulement, env exporté du cluster plutôt que des manifestes) ; `docs/PRODUCT-V1.md` référencé mais absent du snapshot ; manifeste de référence sans namespace/ingress/netpol/HPA et limites cpu/mémoire non justifiées par des mesures.

## P4 — Instrumentation pour la recherche (dette scientifique)

| # | Manque | Pourquoi c'est bloquant | Statut |
|---|---|---|---|
| I-1 | **Aucun dataset de décisions de routage loggé** (bras choisi, alternatives, récompense réalisée) — la télémétrie est infra-level uniquement. | Toute évaluation empirique du bandit (Phase 4) exige ce log ; le run-record spec §3.8 le fournirait. | fixed-in-research |
| I-2 | Run-record non signé (le "signé par hash" du README est un abus de langage : ce sont des hashes, pas des signatures) ; pas de chaînage par étage ni de vérifiabilité contre le devis. | Nécessaire pour l'angle reçus vérifiables (X10) et pour l'audit quote-vs-realized (axe A). | open |
| I-3 | Aucun signal de qualité de sortie nulle part (le "quality monitoring" = seuil d'erreurs HTTP 0.2) ; la claim "budget preset ≈ quasi-frontier" du council n'a aucune évaluation. | Bloque tout claim qualité en Phase 4 ; nécessite probes/judges. | open |
| I-4 | Aucun benchmark dans multi-lang ; agent_balanced et agent_quality identiques dans le catalogue local (différenciation runtime inconnue). | Baselines nécessaires pour les études des angles C et E. | open |

---

*Prochaine revue : à chaque fin de phase, ou dès qu'une expérience de Phase 4 touche un item (le corriger dans la copie de recherche → statut `fixed-in-research`).*
